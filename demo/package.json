{
  "name": "docktree-demo",
  "private": true,
  "scripts": {
    "record": "npx tsx scripts/record-browser-demo.ts",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0",
    "tsx": "^4.0.0"
  }
}
